name: ADE Verification Caller

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  ade-check:
    uses: First-ADE/.github/.github/workflows/ade-verify.yml@main
